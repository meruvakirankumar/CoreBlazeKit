# Base

Reusable abstract base classes for components.

Planned:
- `BaseInputComponent<T>` — shared logic for all input controls (label, placeholder, disabled, two-way binding, validation, error display, EventCallbacks).
- `BaseComponent` — root class exposing common cascading state (theme, CSS class merging).
