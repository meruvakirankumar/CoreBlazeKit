@namespace CoreBlaze.Components.Grid
@typeparam T where T : class
@* GridColumn is a metadata component — it registers itself with the parent DataGrid<T>
   and has no visual output of its own. All rendering happens inside DataGrid<T>. *@
