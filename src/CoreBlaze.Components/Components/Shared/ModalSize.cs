namespace CoreBlaze.Components.Shared;

/// <summary>Size preset for <see cref="Modal"/>.</summary>
public enum ModalSize
{
    /// <summary>Narrow — good for confirmations.</summary>
    Small = 0,
    /// <summary>Balanced default width (~30rem).</summary>
    Medium = 1,
    /// <summary>Wide — for forms with multiple fields.</summary>
    Large = 2,
    /// <summary>Extra wide — for grids or detail views.</summary>
    ExtraLarge = 3,
}
