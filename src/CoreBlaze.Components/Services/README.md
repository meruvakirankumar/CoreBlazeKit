# Services

Injectable services registered via `AddCoreBlazeComponents()`:
- `ThemeService` — theme switching (optional bonus).
- `ValidationService` — cross-component validation helpers (optional bonus).
- `GridStateService<T>` — grid editing / paging / sorting state (optional bonus).
